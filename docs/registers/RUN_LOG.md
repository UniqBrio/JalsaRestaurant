# Run log

> **What was asked, which kind of request it was, and what it cost.** One row per run,
> **newest first**, append-only — never renumber, never backfill silently, never hard-delete.
>
> **Rows are written by `scripts/run-log.mjs`, not by hand.** The two timestamps are read from
> the machine clock at the moment each event happens. A start time typed in at the *end* of a
> run is a recalled time, and a duration derived from two recalled times is an estimate wearing
> the costume of a record — which is precisely what [RC-008](./ROOT_CAUSE_REGISTER.md) cost
> this framework: "run reports carry stage timings" was a rule for three versions and produced
> **not one measured number**, because the only party asked to honour it was a narrator.

```bash
node scripts/run-log.mjs start --type CHANGE --action "sign-out lands on the wrong screen" --scale micro
node scripts/run-log.mjs stage ground     # then: plan · build · verify · gate
#   … the run happens, marking each stage as it begins …
node scripts/run-log.mjs end --verdict PASS
node scripts/run-log.mjs status           # what is open, which stage, how long
```

**Mark a stage at its start, not its end.** Each stage runs until the next mark, so no
wall-clock falls between two stages unattributed. A total tells you a run was slow; the stage
breakdown is the only thing that tells you *what to fix*.

> **Column added 08-Sep-2026 (v1.25.0): `Stages`.** Rows R-001 and R-002 predate it and carry
> `-`. Their content is unchanged — the cell is added, nothing is rewritten — and `-` is the
> honest value, because those runs were never staged. Backfilling a plausible split would put
> invented numbers beside measured ones with nothing to tell them apart.

## The columns

| Column | What it holds |
|---|---|
| **ID** | `R-001`, ascending, never reused |
| **Action** | What the requester asked, **in their words** — not a summary of what was built |
| **Type** | The classification from [`workflows/request.md`](../../workflows/request.md) R1 |
| **Scale** | `micro` · `scoped` · `full-scale` · `n/a` — the lane the run declared |
| **Started · Ended** | Local time, read from the machine clock at each event |
| **Total** | Computed, never typed |
| **Stages** | `ground · plan · build · verify · gate`, each with its own measured duration. Only stages actually marked appear — **an unmarked stage is absent, never `0`**, because zero would claim the stage ran instantly rather than that nobody measured it |
| **Gate** | The gate's own measured cost, lifted from the newest `Time:` line in `TEST_SUMMARY.md` |
| **Verdict** | `PASS` · `FAIL` · `BLOCKED` — the three the gate has; there is no fourth |
| **Notes** | `back-filled start` when a row's start was supplied rather than measured, plus anything worth a phrase |

**Type** uses the same vocabulary as `/request` R1, deliberately — a second set of names for
one concern means two different answers to "how many bug runs did we do".

| Type | In plain words |
|---|---|
| `NEW-APP` | a whole new application |
| `NEW` | a new feature, in an app that already exists |
| `CHANGE` | a functionality correction — it works, it should behave or look different |
| `BUG` | a defect — erroring, wrong output, wrong data |
| `REFACTOR` | same behaviour, better structure |
| `TRIAGE` · `BRAINSTORM` · `FRAMEWORK` | a list to order · thinking it through · the process itself was repaired |

## Why `Total` and `Gate` sit next to each other

They answer the only question a slow run really raises: **was it the machine or the agent?**
Measured on this repository 08-Sep-2026, the entire mechanical stack — `audit:all` 17.7s,
`guard:test` 60.2s, `npm run gate` 8.6s — is about **87 seconds**. So a two-minute gap between
those two columns is the tooling, and a fifty-minute gap is not. Any proposal to speed up a run
should start by reading this table rather than by guessing which part felt slow.

---

| ID | Action | Type | Scale | Started | Ended | Total | Stages | Gate | Verdict | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| R-038 | Owner > Staff: organise the list into HAS ACCESS / NO ACCESS subtabs, each split into Available today / Unavailable today, keeping the existing role grouping, counts from real data, default Has access. Access and availability are separate states and must not be conflated. | CHANGE | scoped | 2026-09-18 07:11 | 2026-09-18 08:01 | 50m | - | 3.4s | BLOCKED | Owner > Staff organised into Has access / No access, each split Available today / Unavailable today with role grouping preserved inside each section. 591 unit + 316 render pass; tsc, lint, build, audit:all (10/10), guard:test (15/15) all clean. Gate 11 PASS / 0 FAIL / 1 BLOCKED - G8 functional skipped, no sanctioned seeded database in this container. Uncommitted by request. |
| R-037 | Owner > Staff > Module access: changing a staff member's permissions shows 'Something on our side failed. Nothing you did was lost - try again.' The staff list itself loads correctly. Want the actual root cause, not a UI workaround. | BUG | scoped | 2026-09-18 06:34 | 2026-09-18 07:10 | 36m | - | 3.4s | BLOCKED | ROOT CAUSE, two layers. (1) Environment: 20260917120000_jalsa_drop_ambiguous_set_staff_pin.sql is unapplied to production - verified live, set_staff_pin still has both signatures, so PostgREST cannot resolve the RPC and 'Give them the app' fails. Production's ledger stops at 20260916105856; seven repo migrations unapplied. (2) Code: db-errors.ts listed the DATABASE's SQLSTATEs but the app talks to PostgREST, which answers with its own PGRST203 - so the 'trying again will not help' message never fired for the fault it was written for. FIX: added PGRST202/203/204 to SCHEMA_FAULTS. Nothing hidden or swallowed. Evidence first: replayed the exact setPermissions sequence on the TEST project (all succeeded), confirmed schema/RLS/triggers/grants/catalogue all clean, proved it has NEVER worked (granted_by='setup' everywhere, zero Permission audits) while other owner writes work today. 12 new cases; fail-first observed. audit:all 10/10, guard:test 15/15, tsc, eslint, build, 572 unit, 308 render, 5 degraded functional PASS. BLOCKED on G8. NOT VERIFIED: no failure mechanism found in setPermissions itself - needs the Vercel log or a test-project key. Two files changed, both module-access. Nothing committed or pushed. |
| R-036 | Indoor Queue QR + Waitlist management: a dedicated entrance QR distinct from the table QRs, owner/captain open-close control, party size 1-10+, token, live position, welcome-to-table handing off into the existing ordering flow, a queue-closed screen that does not cancel anyone, a Waiting outside operator view showing requests AND guests, and captain access through the existing module-access permissions. | NEW | full-scale | 2026-09-18 06:17 | 2026-09-18 06:33 | 16m | - | 3.4s | BLOCKED | PHASE 1 found most of this built: waitlist_entry, the shared W- series, the four-state entrance page, the cookie-isolated guest API, six granular queue permissions, the owner waitlist screen, queue.open, audit, useLiveData and the QR generator. Four gaps closed: (G1) guestJoinQueue never read queue.open so a stale tab or direct POST joined a closed queue - now refused before a token is taken, 409 at the route, closed screen on the phone; (G2) no entrance QR existed - one generator now serves both codes and Tables & QR gained an Indoor queue code card; (G3) the operator headline hid the party structure - now 'N requests, M guests'; (G4) the queue screen ran its own setInterval - now useLiveData. NO MIGRATION REQUIRED. 22 new unit cases; four injected defects observed failing; two probe defects and one real testid gap found and fixed. audit:all 10/10, guard:test 15/15, tsc, eslint, build, 565 unit, 308 render, 5 degraded functional PASS. BLOCKED on G8 only. Eight files touched, all queue. Nothing committed or pushed. |
| R-035 | Owner/Admin > Settings > Restaurant details: improve the logo area so the saved logo stays visible, a Browse/Change control replaces it from the device, SVG/PNG/JPEG only, max 15 MB, preview before saving, and the existing logo survives a cancel or a failure. THIS WORKSTREAM ONLY - not the guest-session fix, combobox, heard_about, promotions, Uplift, payments, WhatsApp or staff ordering. | NEW | scoped | 2026-09-18 06:12 | 2026-09-18 06:16 | 4m | - | 3.4s | BLOCKED | Inspection settled the question the brief asked: restaurant.logo_url has existed since the core schema (line 80, default '/brand/jalsa-badge.png') but at HEAD guest-view.ts reads it ZERO times, SettingsSection.tsx has no logo control at all, and four source files hardcode the bundled path. So the logo was persisted data by schema and a static bundled asset in practice. The implementation satisfying all twelve points was built earlier this session and was VERIFIED this run rather than rebuilt: 34 unit + 41 render cases re-run green, tsc, eslint, audit:all 10/10, guard:test 15/15, 543 unit, build emits /api/owner/logo. One storage-bucket migration was required and is applied to NO environment. Scope: the three logo-owned source files reference none of the excluded workstreams; six shared files carry logo hunks alongside other pending work, which cannot be unmixed in the tree without discarding work earlier briefs said to leave uncommitted - reported rather than done. Nothing committed or pushed. |
| R-034 | Implement a configurable CUSTOMER PROMOTIONS / SPECIALS system: Owner/Admin defines reusable promotions (name, description, supporting text, member menu items via the shared combobox); Day Setup activates which are live today and in what order, max 5; the guest sees only active promotions above the normal menu and orders through the existing cart/round/KOT/bill flow; items added FROM a promotion are attributed to that promotion for Uplift, while the same item added from the Menu is not. | NEW | full-scale | 2026-09-18 05:30 | 2026-09-18 06:11 | 41m | - | 3.4s | BLOCKED | Built: promotion + promotion_item tables with a 5-active trigger and a pair primary key, item_source extended with 'promotion' plus a promotion FK and a snapshot name on kot_item, a Specials panel in Settings (definition, shared combobox composed with chips), a Specials block in Day setup (activation, order, empty state to Settings), PromotionSection above the guest menu reusing MenuRow, and an itemised app-driven uplift breakdown. 64 new cases (29 unit + 35 render); six injected defects observed failing; four quick-add assertions re-expressed against the generalised code rather than weakened. audit:all 10/10, guard:test 15/15, tsc, eslint, build, 543 unit, 308 render, 5 degraded functional all PASS. Migration applied to TEST only; sixth activation refused by the trigger, duplicate membership refused by the key, promotion delete left all 57 menu items intact; cleaned up. Production re-verified untouched. BLOCKED on G8: the full journey needs a non-production service-role key this container does not have. Nothing committed or pushed. |
| R-033 | Implement a customer-facing QUICK ADD for a Water bottle on the Jalsa table home/welcome screen. The action is an ORDER action, not merely a request to the captain — Water bottle, quantity 1, added to the current bill/order round, reusing the existing menu item/order mutation and bill logic. Only quick-add water bottles are attributed to Uplift; normal Menu sales stay normal. | NEW | n/a | 2026-09-18 04:39 | 2026-09-18 05:29 | 50m | - | 3.4s | BLOCKED | Gate 1 found no Water bottle menu item at all (0 of 57, both projects); requester chose owner-nominates-in-Menu. Built: quick_add on menu_item with a one-per-restaurant partial unique index, a new item_source enum on guest_cart_line and kot_item, QuickAddCard on the guest welcome screen reusing the existing cart collapse and Stepper, setQuickAddItem plus a One tap column in the owner Menu, and one Uplift tile. 60 new cases (20 unit + 40 render) green; five injected defects observed failing. audit:all 10/10, guard:test 15/15, tsc, eslint, build, 514 unit, 273 render, 5 degraded functional all PASS. Migration applied and exercised on the TEST project only (57/37/19 rows unchanged, second nomination refused with 23505, cleaned up); production re-verified untouched. BLOCKED on G8: the full functional tier needs a non-production service-role key this container does not have. Nothing committed or pushed. |
| R-032 | Implement logo upload/change under Owner → Settings → Restaurant details: show the current logo, Browse to replace it, SVG/PNG/JPEG only, max 15 MB, preview before saving, and never destroy the working logo when an upload fails. | NEW | full-scale | 2026-09-18 04:16 | 2026-09-18 04:38 | 22m | - | 3.4s | BLOCKED | Built: src/lib/logo-upload.ts (byte-level validator), src/app/api/owner/logo/route.ts (multipart, server-derived path), setRestaurantLogo in owner-mutations, LogoCard in SettingsSection, logoUrl on the guest payload and its three consumers, and a bucket migration. 75 new cases (34 unit + 41 render), all green; five injected defects observed failing. audit:all 10/10, guard:test 15/15, tsc, eslint, build, 494 unit, 233 render, 5 degraded functional all PASS. BLOCKED on G8: the end-to-end upload needs a service-role key for a non-production Supabase project and this container has only the production one; the bucket migration is applied nowhere. Recorded in TEST_SUMMARY.md as NOT EXECUTED, not as passing. Nothing committed or pushed, as the brief required. |
| R-031 | Redesign Owner → Settings → Restaurant details to follow the customer-facing Jalsa visual language: mobile-first, grouped rounded cards, a maroon brand area with the logo. UI only — preserve every field, the save mutation and all validation. | CHANGE | scoped | 2026-09-18 04:09 | 2026-09-18 04:16 | 7m | - | 3.4s | BLOCKED | Restaurant details recomposed: brand block + five grouped cards, one column on a phone and two from md up. Presentation only - same ten columns, same form state, same test ids, same single write-identity patch, same frozen sentences. 29 new cases; fail-first 3 injected field losses (1/10 each). Honest correction: the old flex-wrap layout did NOT overflow at these widths - the render spec is a regression guard, not a fix. 652/652 tiers, audit 10/10, tsc+eslint+build clean. BLOCKED: database-backed functional still unavailable. NOT committed, NOT pushed. |
| R-030 | Apply 20260918030000_jalsa_guest_heard_about.sql to the TEST project only, verify the schema, then run the functional validation that was previously blocked. Do not touch production. | CHANGE | scoped | 2026-09-18 04:02 | 2026-09-18 04:08 | 7m | - | 3.4s | BLOCKED | Migration applied to TEST only and verified: 70 rows intact, one column/index/constraint added, default landed on all 70, a 61-char write refused with 23514, 60 accepted, test values cleaned up. Production re-checked: heard_about absent, untouched. Degraded functional 5/5 PASS for real and its trace shows resolveGuest running. 623/623 tiers, audit 10/10, tsc+eslint+build clean. STILL BLOCKED: no test-project service-role key in this container, and RLS with no policy means a publishable key authorises nothing, so the app cannot be pointed at the test project. The 17 combobox interaction checks are NOT done and were not faked. |
| R-029 | Implement a robust single-QR / multiple-customer session model: one permanent QR per table; the bill belongs to the guest session, not the table; a closed bill must not make the table occupied, and the old phone must keep showing its own closed bill while a new customer orders on the same table. | CHANGE | full-scale | 2026-09-18 03:52 | 2026-09-18 04:01 | 9m | - | 3.4s | BLOCKED | Root cause: both guest resolvers picked the bill by TABLE and wrote it onto the asking session, so after a turnover customer 1 saw and could write to customer 2 live bill. Decision extracted to src/lib/guest-phase.ts (pure); both resolvers call it via one settle(); table consulted only when the session has no bill; lastClosedBillForTable deleted. Explicit Start a new visit route + control added. 27 new unit cases; fail-first 9/19 and 5/8. 623/623 tiers, audit:all 10/10, tsc/eslint/build clean. BLOCKED: functional tier not run, so two-phones-one-table is NOT observed end to end; heard_about migration still unapplied. NOT committed, NOT pushed. |
| R-028 | Proceed with implementation. Design and implement a shared Jalsa searchable combobox; migrate Menu Category (search+create, real menu_category row), Expenses Category (search+create), Printer (search only), LiveOrders staff picker (search only), and add guest "How did you hear about us?" (search+create). Keep the 11 static selects and the filter system as they are. | NEW | full-scale | 2026-09-18 03:19 | 2026-09-18 03:34 | 15m | - | 3.4s | BLOCKED | One shared combobox (src/components/ui/combobox.tsx); five fields migrated: menu category (search+create, real menu_category row via addCategory which now returns the id and reuses on 23505), expense category (search+create, string contract unchanged), printer route (search only), staff reassign (search only, hand-rolled picker removed), guest how-did-you-hear (search+create, new guest_session.heard_about). 11 static selects and the filter system untouched. 25 new unit cases; fail-first 3 injected defects + 7/13. 604/604 tiers, audit:all 10/10, tsc/eslint/build clean. BLOCKED: functional tier not run (real-data), so keyboard/mount behaviour and the 13-width dropdown check are NOT covered; migration applied nowhere. NOT committed, NOT pushed. |
| R-027 | Coordinated functional-flow change: replace direct customer UPI payment with "Request payment" to captain/waiter + owner; fix WhatsApp invoice PDF delivery; add Waitlist QR and Google Review QR; add "How did you hear about us?" combobox on the guest landing screen; audit the same patterns elsewhere. | TRIAGE | full-scale | 2026-09-18 03:14 | 2026-09-18 03:16 | 2m | - | 3.4s | PASS | Five items, audited end-to-end, nothing implemented. Findings change three of them: (1) payment - the UPI journey is a LABELLED stand-in, pay never closes the bill, and request-payment already exists and already reaches the owner; a simplification, not a bug fix. (2) WhatsApp - not broken, never built: both buttons only show a toast, Settings says 'Needs a provider before it can actually send', no PDF lib, no bucket, no customer phone column anywhere. (3) QR - both destinations already exist (/q is the door code, engagement.reviewUrl is configured and already used); only the images and a purpose param are missing. (4) How did you hear - blocked on the shared combobox, which was audited 17-Sep and does not exist. Queue: QR -> payment -> combobox -> attribution -> WhatsApp. |
| R-026 | In "Closed today" section, on click of the bill number, the whole order details including GST should be shown up. In next screen, add "Print" and "Share to whatsapp" button. | NEW | scoped | 2026-09-17 17:37 | 2026-09-17 17:48 | 11m | - | 3.4s | BLOCKED | Closed today: the bill number opens the full bill - rounds, lines, per-table, totals with GST, and how it was settled - with Print and Share to whatsapp. OwnerBillView gained four closure fields it was already fetching; no query, no migration. Share is a wa.me link from the owner's own device, NOT an outbound send. 20 new unit cases; fail-first 4 injected defects + 8/9. 579/579 tiers, audit:all 10/10. BLOCKED: functional tier, same real-data reason. |
| R-025 | In "Closed today", on click of the bill number show the whole order details including GST; next screen add Print and Share to whatsapp. In "Report" section nothing is showing up, fix the bug; same bill/invoice drill-down there. In Audit log, whatever action done by the staff are not included. Fix them. | TRIAGE | scoped | 2026-09-17 17:34 | 2026-09-17 17:36 | 2m | - | 3.4s | PASS | Three items. Queue: (1) BUG reports-shows-nothing - permanent loading, NOT reproduced here (only app instance points at real data); both failure paths in ReportsSection write text, so the promise never settles - that is where Track C starts. (2) NEW bill-detail-print-share - one screen, two entry points; content already exists in OwnerBillView; Share to whatsapp hits the outbound-send deny-by-default floor. (3) BUG audit-log-staff-actions - intake DISPROVES the stated defect: all 11 staff verbs audit, nothing is filtered out, and the same owner is signed in on both tabs; likely the KOT dead end or an expectation about names. Nothing implemented. |
| R-024 | In "Record payment" screen, show the order details on the left pane. | CHANGE | scoped | 2026-09-17 17:22 | 2026-09-17 17:34 | 12m | - | 3.4s | BLOCKED | Record payment: order details on a left pane, money and form on the right; one column below md. Data was already in OwnerBillView.kots[].items[] - no query, route or migration. 8 unit + 27 render cases; fail-first 5/8 and 9/27. The render spec found a real phone-width spill (auto track sizing to max-content) that the class assertion could not - fixed with grid-cols-1. 559/559 tiers, audit:all 10/10, tsc+eslint clean. BLOCKED: functional tier not run, same real-data reason as the previous run. |
| R-023 | Add image browse option to add an image in a menu item. Implement a Jalsa-wide standardized SEARCHABLE COMBOBOX pattern. | TRIAGE | full-scale | 2026-09-17 17:04 | 2026-09-17 17:07 | 3m | - | 3.4s | PASS | Two unrelated items, both NEW. Phase 1 audit complete: 13 <select> call sites (all one shared primitive, all owner console), 1 datalist, 1 filter popover system, 1 hand-rolled staff picker. Only 3 fields are real combobox candidates; 11 stay per the brief's own Phase 2C. KEY FINDING: the reference interaction the brief says already exists does NOT exist - no combobox anywhere in src/. Queue: (1) menu-item image, (2) searchable combobox. Nothing implemented; not committed. |
| R-022 | Add a button "See my orders" to see it quick and easy for them to reorder the paste orders quick(They can add favorite now) | NEW | scoped | 2026-09-17 16:48 | 2026-09-17 16:49 | 43s | - | 3.4s | BLOCKED | Intake only. requests/2026-09-17-see-my-orders.md written. BLOCKED at Gate 1 on four questions, two of them load-bearing: favourites are React.useState and are not stored anywhere (GuestProgress.tsx:79), and 'past orders' across visits needs an identity Jalsa deliberately does not have. Nothing built. |
| R-021 | Anything added from this screen(share the love), mark it as "Take away" in Staff screen. I clicked on "Need water" from customer screen and the same is not showing up correct in "Staff" screen(Table A5 No note · waiting). For all requests from "Ask for something" screen, the information is not passed to staff screen. | TRIAGE | micro | 2026-09-17 16:41 | 2026-09-17 16:42 | 1m | - | 3.4s | PASS | Two items, both NEW (nothing ships either). Queue: (1) BUG ask-details-not-on-staff-screen - no schema change, three provable drops, unblocks a floor surface tonight; (2) CHANGE share-the-love-takeaway - needs a migration, so it is the slower one. No dependency between them; ordered by cost, not by sequence. |
| R-020 | Remove "Pay Rs. 200" button and change to "Continue ordering" and add tip button in the place of "Continue ordering" button. Remove "No thanks, continue to payment" button. Ensure the page is responsive. | CHANGE | scoped | 2026-09-17 16:10 | 2026-09-17 16:41 | 31m | - | 3.4s | BLOCKED | Upsell action bar: Continue ordering (primary) + Add a tip (secondary), two stacked full-width buttons; the nested flex row is gone. ACTION_BAR_STACK extracted so the render spec measures what ships. 6 unit + 26 render cases; fail-first 5/6 and 20/26 observed failing. tsc clean, eslint clean, audit:all 10/10, unit+render tiers 524/524. BLOCKED: the functional tier did not run - the dev server points at the only copy of real data. Started it by mistake, killed after ~3min, verified by timestamp it wrote 0 rows. |
| R-019 | Sent to the kitchen never changes. Enable a button to mark it Order ready/received so it is clear the order is served. When Request payment is clicked, mark all items as served. | CHANGE | scoped | 2026-09-17 16:06 | 2026-09-17 16:10 | 4m | - | 3.4s | BLOCKED | KOT status dead end: intake complete, requests/2026-09-17-kot-status-dead-end.md written; reclassified CHANGE->BUG at intake (this row's type is the start-time classification, the request file is authoritative). Paused at the Track C gate on the open question of who attributes 'served'. Superseded mid-turn by the guest closure action-bar request. |
| R-018 | Captain/Waiter picker lists every active person and the server never checks the role; an unsettled tip can be moved to a cleaner | BUG | scoped | 2026-09-17 15:59 | 2026-09-17 15:59 | 0s | - | 3.4s | PASS | canHoldBillRole/eligibleForBillRole in status.ts; picker and reassignBillStaff both call it; search added. 9 cases, fail-first 6 failed/3 passed. |
| R-017 | Complete responsive design audit + remediation across the entire Jalsa application, 13 viewports, all current-release surfaces | CHANGE | full-scale | 2026-09-17 11:28 | 2026-09-17 11:34 | 6m | - | 3.4s | PASS | Reachable surface swept clean at 13 widths (79 checks). Component-first: all 4 hand-rolled chip scrollers eliminated. Signed-in screens unreachable - reported, not claimed. |
| R-016 | Settings submenu: after Customer engagement, Printers and machines is not visible because the submenu navigation is effectively a constrained/non-wrapping horizontal row | BUG | scoped | 2026-09-17 11:15 | 2026-09-17 11:23 | 8m | - | 3.4s | PASS | Settings submenu wraps. Root cause: flex with no flex-wrap inside overflow-x:auto with the scrollbar hidden. Fix is one container class, CHIP_NAV_WRAP. 19 render cases at 8 widths; fail-first 15 failed/4 passed. |
| R-015 | Why table 5 only showing as available? Make every table available as all of them free by default. Enable search option, show only captains and similarly, show only waiters with search bar. | TRIAGE | micro | 2026-09-17 11:09 | 2026-09-17 11:15 | 5m | - | 3.4s | PASS | Queue approved: (1) picker ignores role + server does not validate - BUG, (2) picker search, (3) all-tables landing page. Paused unbuilt - superseded mid-turn by the Settings submenu responsive defect. |
| R-014 | Show 'Discount % and Discount in Rs' in one row as two fields. Show after discount amount in the same screen before 'Paid by' options. Make similar corrections in captain's payment closure screen too. | CHANGE | scoped | 2026-09-12 17:32 | 2026-09-12 17:39 | 7m | - | 4.3s | FAIL | discount row layout + after-discount figure; gate 10/11, G8 the known no-egress limitation |
| R-013 | Update the Owner Menu screen to replace the current common/global filter with column-wise filtering. Filter icon in each column header opening a compact dropdown; filters independent and combinable; active-filter badge with Clear all; keep sorting separate. | CHANGE | scoped | 2026-09-12 16:51 | 2026-09-12 17:01 | 10m | - | 4.3s | FAIL | menu column filters; gate 10/11, G8 the known no-egress limitation |
| R-012 | Allow owner to modify the Captain or Waiter name in any bill while it is running or after it is closed. Add Discount in Rs below Discount %, always synchronized, on Owner Payment Closure and captain Close bill screens. | CHANGE | scoped | 2026-09-12 13:09 | 2026-09-12 13:22 | 14m | - | 4.3s | FAIL | discount both screens + reassign captain/waiter + cancel notice; gate 10/11, G8 the known no-egress limitation |
| R-011 | allow owner to mark the table free manually; owner can grant it to someone else in RBAC like captains. Sometimes 'something went wrong' error appears, fix it permanently. | CHANGE | scoped | 2026-09-12 09:56 | 2026-09-12 10:07 | 11m | - | 4.3s | FAIL | free-a-table + stale banner; gate 10/11, G8 the known no-egress limitation |
| R-010 | For adding an item, it is taking time. Find the root cause and fix it. The order should be added immediately. | BUG | scoped | 2026-09-12 09:45 | 2026-09-12 09:54 | 9m | - | 4.3s | FAIL | cart latency; gate 10/11, G8 the known no-egress limitation |
| R-009 | Enable a checkbox at the bottom left to show price(Total only). By default, disable it, let the customer click on it to see it. The default setting can be done in Settings screen by the owner to show/hide total order value(without GST). | CHANGE | scoped | 2026-09-12 08:24 | 2026-09-12 08:49 | 25m | - | 4.3s | - | - |
| R-008 | SDL post-generation quality review of the generated Jalsa application, and feed the learnings back into the framework | REVIEW | full-scale | 2026-09-11 07:18 | 2026-09-11 07:26 | 8m | - | 4.3s | PASS | - |
| R-007 | Do not create the design, start with 'Design planning documentation' designs already created. This a new app development request but with no design regeneration. Exclude design part just start implementing using these tech stack: Next.js + TypeScript + React + Supabase + shadcn/ui + PWA. Don't redesign for Next.js. Implement the existing design in Next.js. Add the missing technical states and responsive behavior during implementation. And do not add Apollo GraphQL or another backend language unless a future architectural requirement genuinely calls for it. | NEW-APP | full-scale | 2026-09-10 06:46 | 2026-09-10 14:34 | 7h 48m | - | 4.3s | PASS | - |
| R-006 | Audit log whenever RBAC is enabled: super admin creates an account, assigns a role, customizes features. Reusable component, table view with search, filter by date and module, and sorting | NEW | full-scale | 2026-09-08 13:45 | 2026-09-08 13:54 | 8m | ground 6m · verify 2m · gate 14s | 6.6s | BLOCKED | v1.28.0; CP-27 audit trail; 19/19 audit spec with 2 injected defects observed failing; gate BLOCKED on G5-G8 pre-existing |
| R-005 | Remove the request pre-sorter: it cannot classify reliably when one chat carries more than one request | FRAMEWORK | scoped | 2026-09-08 12:46 | 2026-09-08 12:48 | 2m | ground 9s · build 42s · verify 59s · gate 13s | 6.3s | BLOCKED | v1.27.0; pre-sorter withdrawn; gate BLOCKED on G5-G8 pre-existing |
| R-004 | Validation testing time is high and unmeasured (each correction tested individually); and Supabase soft-deletes where the frontend expects a hard delete | FRAMEWORK | scoped | 2026-09-08 12:22 | 2026-09-08 12:29 | 7m | ground 44s · plan 52s · build 3m · verify 2m · gate 7s | 6.2s | BLOCKED | v1.26.0; CP-26 delete contract, per-commit verification, request pre-sorter; gate BLOCKED on G5-G8 pre-existing |
| R-003 | Reduce execution time: instrument the four unmeasured stages, and make the review matrix executable | FRAMEWORK | scoped | 2026-09-08 10:57 | 2026-09-08 11:21 | 25m | ground 13m · plan 0s · build 7m · verify 3m · gate 14s | 7.0s | BLOCKED | v1.25.0; four speed levers; gate BLOCKED on G5-G8, pre-existing no local tsc |
| R-002 | Write a simple audit log file: action name, request type, start, end, total time taken | FRAMEWORK | scoped | 2026-09-08 10:15 | 2026-09-08 10:42 | 27m | - | 6.1s | BLOCKED | back-filled start; v1.24.0; this register; same pre-existing G5-G8 block |
| R-001 | Correction time is short, but verification takes significantly longer - often exceeding one hour | FRAMEWORK | scoped | 2026-09-08 09:37 | 2026-09-08 10:42 | 1h 05m | - | 6.1s | BLOCKED | back-filled start; v1.23.0; RC-008; gate BLOCKED on G5-G8, no local tsc |