/**
 * FIXTURE FILE — DO NOT "FIX".
 *
 * The hard-coded colours below are DELIBERATE, LOAD-BEARING debt. This fixture exists to prove
 * that a new or stricter gate arrives BASELINED — that an existing app with accepted violations
 * stays green. Cleaning this file up deletes the very condition the conformance suite tests.
 */
export function LegacyCard() {
  return (
    <div style={{ background: '#FFFFFF', color: '#1F2430', border: '1px solid #D6DAE3' }}>
      <button style={{ color: '#4F46E5' }}>Legacy action</button>
    </div>
  );
}
